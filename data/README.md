# Data directory

Real datasets and generated outputs do not belong in Git.

At runtime, mount source data read-only and write derived artifacts to an external ywang-owned output directory. This directory exists only to document that contract.
