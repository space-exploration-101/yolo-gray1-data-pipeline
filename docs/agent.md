# Agent notes

Human-facing usage is in `README.md`. Do not add process ledgers or host paths to the README.

- Work only in ywang-owned paths. Treat chenshiwen data as read-only.
- Do not install host Python/Conda, or modify root/Docker/NAS configuration.
- Pause after each authorized step. Remaining public milestones: tag `v0.1.0`, then NAS backup after separate write authorization.
- Process history: Git log, GitHub Actions, and the archived ledger under the H200 validation directory.